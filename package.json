{
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "git add . && git commit -m \"Auto update source\" && git push origin main && vite build && npx gh-pages -d dist"
  }
}
